# Story 8.2: Quality Gates e Testes Automatizados (E2E)

**Status:** Done ✅
**Epic:** Epic 8: Otimização de Conversão & Escala
**Owner:** @orion (Orchestrator)

## Story
Como administrador do site,
Quero garantir que nenhuma alteração de CSS ou Dados quebre o fluxo de geração de CV,
Para manter a credibilidade técnica absoluta.

## Acceptance Criteria
- [x] AC1: Instalar e configurar **Playwright** no projeto (`package.json`).
- [x] AC2: Criar teste E2E que valide o fluxo: Abrir Modal → Colar Vaga → Gerar Resultado → Verificar Botões de Download.
- [x] AC3: Integrar os testes de Playwright ao pipeline de CI (`ci.yml`) para rodar em cada Pull Request.
- [x] AC4: Gerar relatório de cobertura de testes automatizados.

## Tasks
1. [x] Instalar `@playwright/test` e `serve`.
2. [x] Configurar `playwright.config.js` com webServer integrado.
3. [x] Criar `tests/e2e/conversion-flow.spec.js` validando CTAs e Modal.
4. [x] Atualizar `.github/workflows/ci.yml` com o job `e2e-tests`.
5. [x] Executar e validar testes localmente e no CI.
