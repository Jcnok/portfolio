# Story 2.1: Configurar Ambiente de Testes com Jest

## Status
Done ✅

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["manual-review", "jest"]
```

## Story
**As a** equipe de desenvolvimento,
**I want** um ambiente de testes unitários configurado com Jest,
**so that** a lógica de `projects.js` e `skills-chart.js` possa ser validada automaticamente.

## Acceptance Criteria
1. Criar `package.json` na raiz do projeto com script `npm test`. ✅
2. Configurar Jest para ambiente de browser (jsdom). ✅
3. Escrever pelo menos 3 testes unitários para a lógica de filtro em `projects.js`. ✅ (6 testes escritos)
4. O comando `npm test` deve passar com todos os testes. ✅
5. Adicionar `node_modules/` ao `.gitignore` se não estiver lá. ✅ (já estava)

## Tasks / Subtasks

- [x] Tarefa 1: Criar package.json e instalar Jest (AC: 1, 2)
- [x] Tarefa 2: Tornar ProjectManager testável com conditional export (AC: 3)
- [x] Tarefa 3: Escrever testes unitários (AC: 3, 4)
- [x] Tarefa 4: Executar `npm test` e validar (AC: 4)

## Dev Notes

### Resultado dos Testes
```
PASS  tests/projects.test.js    
  ProjectManager             
    ✓ filterProjects("all") deve mostrar todos os projetos (98 ms)
    ✓ filterProjects("machine-learning") deve mostrar apenas projetos ML (47 ms)
    ✓ filterProjects com categoria inexistente deve esconder todos os projetos (19 ms)
    ✓ loadProjects deve renderizar o número correto de project cards (17 ms)
    ✓ createProjectCard deve gerar card com todos os elementos (20 ms)
    ✓ deve exibir mensagem de erro quando fetch falha (6 ms)

Test Suites: 1 passed, 1 total
Tests:       6 passed, 6 total
```

### Decisões Técnicas
- **Conditional export**: `if (typeof module !== 'undefined' && module.exports)` no final de `projects.js` para não quebrar o browser
- **Mock fetch**: `global.fetch = jest.fn()` para simular resposta assíncrona no constructor
- **6 testes** (superou o AC de 3 mínimos): filter all, filter ML, filter empty, loadProjects count, createProjectCard structure, fetch error handling

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-07 | 1.0 | Story criada e implementada | @dev (Orion) |

## Dev Agent Record

### Agent Model Used
Antigravity (Gemini)

### Completion Notes List
- Criado `package.json` com Jest + jest-environment-jsdom
- Adicionado conditional export ao `assets/js/projects.js` (linhas 130-133)
- Criado `tests/projects.test.js` com 6 testes cobrindo filtros, renderização e error handling

### File List
- `package.json` — Criado (scripts: npm test)
- `package-lock.json` — Gerado automaticamente
- `assets/js/projects.js` — Modificado (conditional export adicionado)
- `tests/projects.test.js` — Criado (6 testes)
